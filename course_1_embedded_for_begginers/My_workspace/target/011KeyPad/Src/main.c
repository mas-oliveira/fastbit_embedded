/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2026 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdio.h>
#include <stdint.h>

#include "main.h"

int main(void)
{
	RCC_AHB1ENR_t volatile *const rcc_ahb1en_reg   = (RCC_AHB1ENR_t *) RCC_AHB1ENR_ADDR;
	GPIOx_MODER_t volatile *const gpioa_mode_reg   = (GPIOx_MODER_t *) GPIOA_MODER_ADDR;
	GPIOx_PUPDR_t volatile *const gpioa_pupdr_reg  = (GPIOx_PUPDR_t *) GPIOA_PUPDR_ADDR;
	GPIOx_IDR_t   volatile *const gpioa_input_reg  = (GPIOx_IDR_t *) GPIOA_IDR_ADDR;
	GPIOx_ODR_t   volatile *const gpioa_output_reg = (GPIOx_ODR_t *) GPIOA_ODR_ADDR;

	//char pressed_key = '\0';

	// Enable the clock of GPIO A
	rcc_ahb1en_reg->RCC_AHB1ENR_BITS.GPIOAEN = 0b01;

	// Set of PA0 (Row 1), PA1 (Row 2), PA2 (Row 3), PA3 (Row 4) pins as OUTPUT (01)
	gpioa_mode_reg->GPIO_MODER_BITS.MODER0 = 0b01;
	gpioa_mode_reg->GPIO_MODER_BITS.MODER1 = 0b01;
	gpioa_mode_reg->GPIO_MODER_BITS.MODER2 = 0b01;
	gpioa_mode_reg->GPIO_MODER_BITS.MODER3 = 0b01;

	// Set of PA4 (Column 1), PA5 (Column 2), PA6 (Column 3), PA7 (Column 4) pins as INPUT (00) - Reset value, this step can be jumped
	gpioa_mode_reg->GPIO_MODER_BITS.MODER4 = 0b00;
	gpioa_mode_reg->GPIO_MODER_BITS.MODER5 = 0b00;
	gpioa_mode_reg->GPIO_MODER_BITS.MODER6 = 0b00;
	gpioa_mode_reg->GPIO_MODER_BITS.MODER7 = 0b00;

	// Enable the pull-up resistor of all input PA4-PA7 pins (01)
	/*gpioa_pupdr_reg->GPIO_PUPDR_BITS.PUPDR0 = 0b01;
	gpioa_pupdr_reg->GPIO_PUPDR_BITS.PUPDR1 = 0b01;
	gpioa_pupdr_reg->GPIO_PUPDR_BITS.PUPDR2 = 0b01;
	gpioa_pupdr_reg->GPIO_PUPDR_BITS.PUPDR3 = 0b01;*/
	gpioa_pupdr_reg->GPIO_PUPDR_BITS.PUPDR4 = 0b01;
	gpioa_pupdr_reg->GPIO_PUPDR_BITS.PUPDR5 = 0b01;
	gpioa_pupdr_reg->GPIO_PUPDR_BITS.PUPDR6 = 0b01;
	gpioa_pupdr_reg->GPIO_PUPDR_BITS.PUPDR7 = 0b01;

	// Init all row pins (PA0 - PA3) HIGH
	gpioa_output_reg->GPIO_ODR_BITS.ODR0 = 0b01;
	gpioa_output_reg->GPIO_ODR_BITS.ODR1 = 0b01;
	gpioa_output_reg->GPIO_ODR_BITS.ODR2 = 0b01;
	gpioa_output_reg->GPIO_ODR_BITS.ODR3 = 0b01;

    /* Loop forever */
	for(;;) {
		gpioa_output_reg->GPIO_ODR_BITS.ODR0 = 0b00; // Selecting Row 1 (PA0 LOW)
		//for(uint32_t volatile i=0;i<200000;i++); // Forced delay
		if (!gpioa_input_reg->GPIO_IDR_BITS.IDR4) { // PA3 (C1) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("1\n");
			//pressed_key = '1';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR4); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR5) { // PA4 (C2) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("2\n");
			//pressed_key = '2';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR5); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR6) { // PA5 (C3) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("3\n");
			//pressed_key = '3';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR6); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR7) { // PA6 (C4) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("A\n");
			//pressed_key = 'A';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR7); // DEBOUNCE Hold
		}
		gpioa_output_reg->GPIO_ODR_BITS.ODR0 = 0b01; // Unselecting Row 1 (PA0 HIGH)
		//for(uint32_t volatile i=0;i<200000;i++); // Forced delay

		gpioa_output_reg->GPIO_ODR_BITS.ODR1 = 0b00; // Selecting Row 2 (PA1 LOW)
		//for(uint32_t volatile i=0;i<200000;i++); // Forced delay
		if (!gpioa_input_reg->GPIO_IDR_BITS.IDR4) { // PA3 (C1) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("4\n");
			//pressed_key = '4';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR4); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR5) { // PA4 (C2) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("5\n");
			//pressed_key = '5';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR5); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR6) { // PA5 (C3) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("6\n");
			//pressed_key = '6';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR6); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR7) { // PA6 (C4) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("B\n");
			//pressed_key = 'B';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR7); // DEBOUNCE Hold
		}
		gpioa_output_reg->GPIO_ODR_BITS.ODR1 = 0b01; // Unselecting Row 2 (PA1 HIGH)
		//for(uint32_t volatile i=0;i<200000;i++); // Forced delay

		gpioa_output_reg->GPIO_ODR_BITS.ODR2 = 0b00; // Selecting Row 3 (PA2 LOW)
		//for(uint32_t volatile i=0;i<200000;i++); // Forced delay
		if (!gpioa_input_reg->GPIO_IDR_BITS.IDR4) { // PA3 (C1) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("7\n");
			//pressed_key = '7';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR4); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR5) { // PA4 (C2) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("8\n");
			//pressed_key = '8';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR5); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR6) { // PA5 (C3) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("9\n");
			//pressed_key = '9';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR6); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR7) { // PA6 (C4) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("C\n");
			//pressed_key = 'C';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR7); // DEBOUNCE Hold
		}
		gpioa_output_reg->GPIO_ODR_BITS.ODR2 = 0b01; // Unselecting Row 3 (PA2 HIGH)
		//for(uint32_t volatile i=0;i<200000;i++); // Forced delay

		gpioa_output_reg->GPIO_ODR_BITS.ODR3 = 0b00; // Selecting Row 4 (PA3 LOW)
		//for(uint32_t volatile i=0;i<200000;i++); // Forced delay
		if (!gpioa_input_reg->GPIO_IDR_BITS.IDR4) { // PA3 (C1) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("*\n");
			//pressed_key = '*';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR4); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR5) { // PA4 (C2) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("0\n");
			//pressed_key = '0';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR5); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR6) { // PA5 (C3) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("#\n");
			//pressed_key = '#';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR6); // DEBOUNCE Hold
		} else if (!gpioa_input_reg->GPIO_IDR_BITS.IDR7) { // PA6 (C4) Low//Pressed
			for(uint32_t volatile i=0; i<300000; i++);
			printf("D\n");
			//pressed_key = 'D';
			//while (!gpioa_input_reg->GPIO_IDR_BITS.IDR7); // DEBOUNCE Hold
		}
		gpioa_output_reg->GPIO_ODR_BITS.ODR3 = 0b01; // Unselecting Row 4 (PA3 HIGH)
		//for(uint32_t volatile i=0;i<200000;i++); // Forced delay
	}
}
